spring:
  datasource:
    url: jdbc:mysql://localhost:3306/group1database
    username: user1
    password: password1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  security:
    user:
      name: user2
      password: password2
  mail:
    host: smtp.outlook.com
    port: 587
    username: IAmChuckNorris1940@outlook.com
    password: hello123123
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  expiration: 3600 # Expiration time in seconds (e.g., 3600 seconds = 1 hour)
  secret: yourSecretKey 

logging:
  level:
    org.hibernate: DEBUG

your:
  custom:
    property: value

server:
  port: 8081
