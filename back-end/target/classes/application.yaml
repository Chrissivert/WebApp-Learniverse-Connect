spring:
  datasource:
    url: jdbc:mysql://localhost:3306/group1database
    username: user1
    password: password1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    root: INFO

your:
  custom:
    property: value

server:
  port: 8081
